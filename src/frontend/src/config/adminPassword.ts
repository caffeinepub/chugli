// This is the single source of truth for the admin password used in the UI unlock flow.
// To change the admin password, update this value here.
// Note: This password is used for UI access control only. Room deletion uses a password
// provided by the user at deletion time, which is validated by the backend.
export const ADMIN_PASSWORD = 'ilovemk2116';
